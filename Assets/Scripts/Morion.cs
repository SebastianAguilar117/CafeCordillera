using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Morion : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print ("hola");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
